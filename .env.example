# Copy to .env and adjust. Only EXPO_PUBLIC_* variables are exposed to the app bundle.
# "mock" uses the in-app mock services; "real" will use the HTTP services once the backend exists.
EXPO_PUBLIC_API_MODE=mock
EXPO_PUBLIC_API_URL=https://api.example.com
